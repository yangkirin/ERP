<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kirin.modules.storage.dao.StorageSearchDao">


    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.kirin.modules.storage.entity.StorageSearchEntity" id="storageSearchMap">
        <result property="id" column="ID"/>
        <result property="mtrName" column="MTR_NAME"/>
        <result property="mtrNo" column="MTR_CODE"/>
        <result property="purchaseUnit" column="PURCHASE_UNIT"/>
        <result property="purchaseUnitRate" column="PURCHASE_RATE"/>
        <result property="miniUnit" column="MINI_UNIT"/>
        <result property="miniRate" column="MINI_RATE"/>
        <result property="formulaUnit" column="FORMULA_UNIT"/>
        <result property="inCount" column="IN_COUNT"/>
        <result property="outCount" column="OUT_COUNT"/>
        <!--<result property="storageCount" column="storage_count"/>-->
    </resultMap>

    <select id="queryList" resultType="com.kirin.modules.storage.entity.StorageSearchEntity" resultMap="storageSearchMap">
        select m.*, SUM(i.IN_COUNT) as IN_COUNT, SUM(O.OUT_COUNT) AS OUt_COUNT from mtr_data m, tb_import_detail i, tb_outport_detail o
        where m.mtr_code = i.mtr_no and m.mtr_code = o.mtr_no
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by ID desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
		select count(*) from (select m.*, SUM(i.IN_COUNT) as IN_COUNT, SUM(O.OUT_COUNT) AS OUt_COUNT from mtr_data m, tb_import_detail i, tb_outport_detail o
        where m.mtr_code = i.mtr_no and m.mtr_code = o.mtr_no) a
	</select>

</mapper>